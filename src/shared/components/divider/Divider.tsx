import styled from '@emotion/styled';

// /. IMPORTS

const StyledDivider = styled.span`
  border: 1.5px dashed #939297;
  margin: 10px 0;
`;

export const Divider = () => {
  return <StyledDivider />;
};
